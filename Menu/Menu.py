'''
conceito do relacionamento

class Cliente:
    
    def __init__(self, nome, tipo, dif, data_nasc)

    id
    self.senha
    self.user = User.pf//pj(nome=nome, tipo=tipo,dif=dif)
    self.conta = Conta()

Cliente('jao')  = 000000x2730, 0002x00123

[{jao:0001x001831},{maria:0002003039904}]

00001,00002,00003

var_logado=0001

logado.user.nome

class Cliente(self,nome,cpf,senha):
    self.cpf=cpf
    self.senha = senha
    self.user = User(nome=nome,cpf=cpf)
    self.conta = Conta()

class User(self,nome,cpf):
    self.nome = nome
    self.cpf = cpf

class Conta:
    self.saldo = 00














obj1, obj2, obj3 

Cliente('maria')
Cliente('jao')

saldo 

var = obj_cliente

















class banco:
    lista_clientes = [cliente1, cliente2]
    
    @classmethod
    def lista(cls):
        return cls.lista_clientes
    
    banco.lista_clientes

class cliente:
    self.cpf = user.cpf
    self.senha = 123
    self.conta = conta()
    self.User = user()

    lista_clientes.append(self)




class interface:
    self.cpf = cliente1.User.cpf
    self.saldo = cliente1.conta.saldo

    '''


































def Criar_user(**kw): #TODO Move and create a new module for this
    if tipo.upper() == 'PF':
        return PF(**kw)
    elif tipo.upper() == 'PJ':
        return PJ(**kw)